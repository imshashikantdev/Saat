var ctx = document.getElementById('myChart').getContext('2d');
var myChart = new Chart(ctx, {
  type: 'bar',
  data: {
    labels: ['US', 'IND', 'RUS', 'BR', 'MEX'],
    datasets: [{
      label: 'TotalConfirmed',
      data: [52, 24, 21, 32, 6],
      backgroundColor: "rgba(153,255,51,0.6)"
    }, {
      label: 'TotalRecovered',
      data: [2, 2, 5, 23, 2],
      backgroundColor: "rgba(255,153,0,0.6)"
    },
    {
        label:'TotalDeaths',
        data: [2, 2, 5, 1, 2],
        backgroundColor: "rgba(153,153,0,0.6)"
    }]
  }
});

//------------------------------------

/*window.onload=function getData(){
    // Create a request variable and assign a new XMLHttpRequest object to it.
    var request = new XMLHttpRequest()
     
    // Open a new connection, using the GET request on the URL endpoint
    request.open('GET', 'https://api.covid19api.com/summary', true)
     
    request.onload = function () {
        // Begin accessing JSON data here
        var data = JSON.parse(this.response);
        console.log(data.Countries[177]);
  
        var Cases = {};
         Cases.Countries= new Array();
  
         for (var i = 0; i < someArray.length; i++) {
          var temp_item = someArray[i];
      
          // Maybe, here make something like:
          // temp_item.name = 'some value'
  
          Cases.Countries.push(
            {
          CountryCode: "US",
          TotalConfirmed: 4941755,
          TotalDeaths: 161347,
          TotalRecovered: 1623870
            },
           {
              CountryCode: "IND",
              TotalConfirmed: 4941755,
              TotalDeaths: 161347,
              TotalRecovered: 1623870
            },
            {
              CountryCode: "RUS",
              TotalConfirmed: 4941755,
              TotalDeaths: 161347,
              TotalRecovered: 1623870
            },
            {
              CountryCode: "BR",
              TotalConfirmed: 4941755,
              TotalDeaths: 161347,
              TotalRecovered: 1623870
            },
  
         )};
        }// forloop
    // Send request
    request.send()
    }
    */